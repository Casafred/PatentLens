# 13 - 专利分享开发交接

> 状态：持续开发中  
> 更新日期：2026-08-04
> 当前功能分支：`feat/share-workspace-foundation`

本文用于后续开发者或 Agent 接手 PatentLens「专利分享」功能。开始开发前应同时阅读：

- [11-专利分享功能设计与开发计划.md](11-专利分享功能设计与开发计划.md)
- [12-专利分享开发规范.md](12-专利分享开发规范.md)
- 根目录 `AGENTS.md`

## 1. 当前完成度

第一版可交付分享闭环已完成。按包含研发洞察、AI、多人协作和高保真输出的完整规划计算，约完成 **60–65%**，其余约 **35–40%**。

已实现：

1. 独立「分享工作台」入口、项目头部及稳定的七个子视图入口。
2. 打开/关闭工作台时保存并恢复既有检索视图可见性；不改变 `searchMode`。
3. 当前 GP 专利结果的只读快照导入，按专利号（不区分大小写）去重。
4. 独立项目状态与 IndexedDB 持久化；存储不可用时只有明确的会话内存降级，不使用 `localStorage` 承载大字段。
5. 数据审核页可显示基础字段、来源，并将人工确认值记录为 `manual` 来源。
6. 字段候选值与优先级合并核心；不同非空来源值会形成显式 `conflict`，审核页可选择候选或人工确认，未决冲突会阻断导出。
7. CSV/XLS/XLSX 导入、常见中英文表头自动映射、未映射自定义列保留，以及与 GP 快照的重复专利合并；Excel 支持工作表选择与 10 MB、20 表、5000 行、100 列上限。
8. 用户选择的 20 MB 以下 PDF 可通过最小化 Electron OCR bridge 解析，并以无原始 PDF 的 OCR 快照关联到指定专利。
9. 必要模块 S1–S5 的 `full/lite` 配置和 R1/R7 默认关闭预留；R7 可输出 OCR 原文摘录，配置会持久化到项目。
10. sandbox iframe 离线预览、HTML 转义、敏感信息扫描及 Electron HTML 保存 bridge。
11. 单元测试、`web-app.js` 冻结门禁和 Electron spreadsheet/OCR bridge smoke 已通过。

仍待实现：导入前手工标题行/列映射、OCR 取消与进度/文本层回退、档案与既有 OCR/AI 历史快照导入、R2–R6/R8/R9 研发洞察、AI 来源核验、逐篇模块覆盖、协作批注、附图或授权 PDF 嵌入、打印模板与大型项目性能优化。

## 2. 当前代码地图

```text
src/scripts/app/share/
  share-entry.js              工作台壳层、导航与当前已实现的概览/来源/审核 UI
  share-field-merge.js        纯字段候选值、优先级、冲突与人工选择逻辑
  share-spreadsheet-import.js CSV/Excel 行数据、表头自动映射、规范化记录生成
  share-module-registry.js    必要/可选模块注册表与配置解析
  share-renderer.js            安全离线 HTML 渲染、预览和敏感信息扫描
  share-project-store.js      分享领域项目、IndexedDB、人工字段确认
  share-source-adapters.js    既有 GP 状态的唯一读取 facade
src/web.html                  入口容器及脚本加载顺序（web-app.js 后）
src/styles/main.css           分享工作台样式
tests/share-project-store.test.cjs
docs/11-专利分享功能设计与开发计划.md
docs/12-专利分享开发规范.md
```

不要修改 `src-tauri/`；不要向 `src/scripts/web-app.js` 添加代码。

## 3. 已落地的数据契约

### 项目

`PatentShareStore.getSnapshot()` 返回隔离深拷贝。项目当前形状：

```js
{
  schemaVersion: 1,
  id, name, createdAt, updatedAt,
  patents: [PatentRecord],
  sources: [{ id, patentId, type, label, capturedAt }],
  moduleConfig: {}
}
```

`PatentRecord` 至少含 `id`、`patentNumber`、`title`、`fields`、`claims`、`ocrSources` 和 `source`。`ocrSources` 只保存 OCR 结果（`fileName`、`engine`、`capturedAt`、`text`、`markdown`、`blocks`、`pageDimensions`），绝不保存 PDF Base64。基础字段以来源值表示：

```js
{
  value: "…",
  source: "manual | excel | google_patents | dossier | pdf_text | ocr",
  sourceRef: "可展示的来源说明",
  capturedAt: "ISO-8601",
  confidence: "high | medium | low",
  reviewState: "accepted | conflict | pending"
}
```

人工确认必须调用 `PatentShareStore.updatePatentField()`，它会覆盖该项目字段为 `manual` 值；不得回写来源缓存。未来的字段合并实现需要在该结构上补充候选值或冲突集合，并保留每个候选值的上述来源语义。

### 持久化

- 数据库：`patentlens-share`，版本 `1`。
- 对象仓库：`projects`（`id` 为 keyPath）和 `meta`（记录 `activeProjectId`）。
- 已实现 API：`initialize()`、`getPersistenceState()`、`getSnapshot()`、`newProject()`、`listProjects()`、`selectProject()`、`renameProject()`、`addPatent()`、`importPatents()`、`updatePatentField()`、`selectPatentFieldCandidate()`、`addOcrSource()`、`removePatent()`、`flush()`、`onChange()`。
- OCR/AI 后续可改为独立对象仓库；当前 OCR 快照存于所属专利记录。无论实现方式如何，都不要把 PDF Base64、密钥、Cookie 或本机路径放入项目记录。

UI 在 `initialize()` 完成后才绑定事件，避免恢复项目与用户操作竞争。写入采用串行队列；需要在测试或窗口关闭路径等待落盘时调用 `flush()`。

## 4. 推荐后续切片

每一项单独提交并完整验证，避免将输入、导出和旧 renderer 重构混在同一 PR。

1. **导入确认与 OCR 完整性**：补充可编辑标题行/列映射、OCR 任务取消和进度、文本层提取/可选引擎回退；保持 20 MB 限制和“不保存原 PDF”约束。
2. **研发洞察模块**：接入 R2/R3/R4/R5/R6/R8/R9，先支持人工编辑，再接入可选 AI；所有 AI 内容必须带来源与人工审核状态。
3. **高级项目审核**：增加逐篇模块覆盖，并补充档案/既有 OCR/AI 历史快照适配器；项目列表应继续补充归档/删除与搜索能力。
4. **协作与高保真输出**：批注、多用户交换、附件/附图授权嵌入、打印模板和大项目性能优化。

## 5. Agent 协作规则

1. 一个 Agent/提交只做一个可验证的垂直切片；不要并发改同一分享文件。
2. 读取既有 GP、dossier、OCR 或缓存只能经 `share-source-adapters.js`（或新增的明确 facade）完成；适配器输出必须是复制后的分享快照。
3. UI 一律用 `textContent` 或经过白名单净化的渲染器写入外来文本；不要把专利、表格或 OCR 内容拼接为 `innerHTML`。
4. 任何导出不得包含 API Key、Cookie、代理地址、绝对本机路径、未经选择的文件或未净化 HTML。
5. 新增脚本必须在 `src/web.html` 的 `web-app.js` **之后**加载；检查 `package.json#build.files` 会将其打入 Electron 包。
6. 修改前后都检查 `git diff -- src/scripts/web-app.js`；该文件不允许新增行。
7. 每个切片至少新增相应单元测试，并运行：

```powershell
npm test
npm run verify:web-app
npm run verify:refactor:full
```

在受限环境中 Electron smoke 可能需要允许写入 Electron 缓存目录；这不是产品代码问题。

## 6. 本轮验证与已知限制

PDF OCR 改动加入后已通过：

```text
npm test                 26 passing
npm run test:electron-smoke  passed
npm run verify:web-app   passed
node --check electron-main.js / preload.js / share-entry.js   passed
git diff --check         passed
```

此前已通过完整 `npm run verify:refactor:full`（含 Electron smoke）；项目列表改动合并前需要重新执行一次作为提交前回归。当前字段冲突 UI、模块配置、预览、导出、项目列表和历史项目重新打开均已实现。项目删除/归档、搜索和协作权限仍未实现，不应把当前能力描述为完整的多用户项目管理。
